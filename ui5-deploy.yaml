# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.1"
metadata:
  name: trackingapp
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: CD4
          url: http://erps42023cd.erp-is.com:4430
          client: '100'
        app:
          name: /SERPCC/TRACKSH
          package: /SERPCC/RAP_SD
          transport: CD4K900025
        exclude:
          - /test/
